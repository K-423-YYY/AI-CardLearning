# AI 闯关学习 · 本地开源版实施指南

> 状态：已实施完成（当前版本 v0.1.4，2026-08-01）  
> 日期：2026-08-01  
> 本文档是 Codex 后续操作的唯一执行依据，实施时按“开工顺序”一节逐项推进。
> 更新：阶段 A-F 已全部完成并发布到 GitHub Releases，当前版本为 v0.1.4。

## 1. 已确认的产品决策

- 产品形态：GitHub 开源的本地优先应用，不上架应用商店。
- 发布渠道：GitHub Releases 提供 Windows 安装包、安卓 APK，另提供可安装的 PWA 网页版。
- 登录：不需要账号，不需要邮箱、密码、验证码；打开应用直接使用。
- 数据：全部保存在用户设备本地，不经过任何服务器，不强制多端同步。
- 删除：删除学习区/卡片/文件即永久清空，不设云端回收站。
- 备份与分享：用户通过导入导出 `.zip` 文件完成，可存放于本地、网盘、云盘，也可直接发给他人。
- 多端关系：一个共享代码库，同一套功能分别打包为 PWA、Windows、安卓。
- 旧服务器版：保留现有 `backend/` 和 `frontend/` 暂不删除，本地版不依赖它们；确认本地版稳定后再决定是否归档。

## 2. 现状盘点

当前项目包含：

- `backend/`：FastAPI + SQLite，包含学习区、卡片、闯关、错题、复习、AI 生成、服务商配置等业务逻辑，以及登录相关代码。
- `frontend/`：原生 HTML/CSS/JS 单页应用，通过 fetch 调用后端 `/api`。
- `AI闯关学习网站完整方案.md`：旧版网站方案文档。

本地版改造的复用点：

- 前端模板、样式、页面结构可以直接复用。
- 后端的数据模型和业务规则需要移植成 JavaScript 本地数据层。
- 手机验证码、邮箱验证码、JWT、Cookie、FastAPI 全部不需要进入本地版。

## 3. 目标架构

```text
同一套前端界面
        │
        ▼
本地数据层（Local Data Layer）
  ├─ 学习区 / 卡片 / 闯关 / 错题 / 复习
  ├─ AI 直连（用户自己的 API Key）
  ├─ 导入导出（.zip）
  └─ 存储适配层
          │
    ┌─────┼──────────┐
    ▼     ▼          ▼
IndexedDB  SQLite    Android SQLite
（PWA）  （桌面）    （安卓）
    │     │          │
    ▼     ▼          ▼
PWA    Electron    Capacitor
        │          │
        ▼          ▼
   Windows      Android APK
```

离线版不需要服务器、不需要域名、不需要备案、不需要短信/邮箱服务。

## 4. 技术选型

- 主应用：保留现有原生 HTML/CSS/JS 技术，避免引入框架重写。
- PWA：`manifest.webmanifest` + Service Worker + IndexedDB。
- Windows：Electron，渲染进程使用 IndexedDB，主进程负责文件对话框，卸载时结束运行中的进程。
- 安卓：Capacitor + IndexedDB，使用系统文件选择器读写网盘/云盘/本地文件。
- AI：前端直接 fetch OpenAI 兼容接口，复用现有 `providers` 配置逻辑。
- PDF：改用 pdf.js 做客户端文本提取。
- 构建：GitHub Actions 分别构建 Windows 安装包和安卓 APK。

## 5. 目标仓库结构

```text
m/
├── 00-实施指南-本地开源版.md
├── app/                         # 本地版主目录（从 frontend 复制改造）
│   ├── index.html
│   ├── manifest.webmanifest
│   ├── sw.js
│   ├── css/
│   └── js/
│       ├── local/
│       │   ├── storage.js       # 存储适配层
│       │   ├── db.js            # 数据表与迁移
│       │   ├── zones.js         # 学习区业务
│       │   ├── cards.js         # 卡片/闯关/复习业务
│       │   ├── ai.js            # AI 直连
│       │   ├── pdf.js           # PDF 文本提取
│       │   ├── export.js        # 导出
│       │   └── import.js        # 导入
│       ├── zones.js             # 页面逻辑（复用后改造）
│       ├── cards.js
│       ├── settings.js
│       └── app.js
├── desktop/                     # Electron 外壳
│   ├── main.js
│   ├── preload.js
│   └── package.json
├── android/                     # Capacitor 安卓外壳
├── docs/
│   ├── 用户使用说明.md
│   ├── 开发者构建指南.md
│   └── 数据格式说明.md
├── samples/                     # 示例学习区备份文件
├── .github/workflows/build.yml  # 自动构建 Releases
├── LICENSE
└── README.md
```

`backend/` 和旧 `frontend/` 在本地版稳定前保持不动。

## 6. 数据与导出格式规范

导出文件统一为单个 `.zip`，文件名示例：

```text
AI闯关学习备份-2026-08-01.zip
```

内部结构：

```text
AI闯关学习备份-2026-08-01.zip
├── manifest.json          # 格式版本、应用版本、导出时间、导出类型
├── data.json              # 学习区、卡片、记录、设置等结构化数据
└── files/                 # 原始上传文件
    ├── file_12.pdf
    └── file_15.py
```

`manifest.json` 示例：

```json
{
  "format_version": 1,
  "app_version": "0.1.4",
  "exported_at": "2026-08-01T12:00:00+08:00",
  "export_type": "full",
  "source_app": "ai-learn-local"
}
```

`export_type` 取值：

- `full`：全量备份，包含所有学习区、卡片、进度、复习计划、非敏感设置。
- `zone`：单个学习区分享，包含该学习区的文件、卡片、进度。

导入规则：

- 导入前校验 `format_version`，不兼容时提示升级应用或拒绝导入。
- 全量备份支持“整包导入”或“选择学习区导入”。
- 同名学习区提供三种处理：覆盖、保留两份、跳过。
- 单学习区导入时，可选择“保留原作者进度”或“重新闯关”。
- API Key 默认不导出，也不随备份文件传输。
- 导入完成后立即生效，全程离线。

## 7. 实施阶段与任务清单

### 阶段 A：本地数据层

- [x] 新建 `app/`，从 `frontend/` 复制基础代码。
- [x] 定义 IndexedDB 数据表，覆盖现有 SQLite 全部业务表。
- [x] 移植学习区、文件上传、卡片、闯关、错题、复习、签到逻辑。
- [x] 移植服务商与模型配置，API Key 仅存本地并可本地加密。
- [x] 移植 AI 调用逻辑，支持 DeepSeek/OpenAI 兼容接口。
- [x] 接入 pdf.js，替换 pypdf。
- [x] 移除登录/注册/忘记密码页面，应用打开直接进入首页。
- [x] 前端所有 `/api/*` 调用替换为本地数据层调用。
- [x] 验收：无后端启动，应用可完整走通“建学习区 → 导入文件 → 生成卡片 → 闯关”。

### 阶段 B：导入导出

- [x] 实现全量导出 `.zip`。
- [x] 实现单个学习区导出 `.zip`。
- [x] 实现 `.zip` 导入。
- [x] 实现格式版本校验。
- [x] 实现同名学习区冲突处理。
- [x] 实现“重新闯关 / 保留进度”选项。
- [x] 确认 API Key 不进入导出文件。
- [x] 验收：导出后删除数据再导入，数据可完整恢复；两个用户之间可互导学习区。

### 阶段 C：PWA

- [x] 添加 `manifest.webmanifest` 和应用图标。
- [x] 添加 Service Worker，实现离线可用。
- [x] 支持浏览器安装提示。
- [x] 添加“导出备份”提醒，防止用户清缓存后丢失数据。
- [x] 验收：Chrome/Edge 可安装，断网后仍可打开和使用已缓存数据。

### 阶段 D：Windows 桌面版

- [x] 创建 Electron 主进程与 preload。
- [x] 接入 better-sqlite3，数据库存放在系统用户数据目录。
- [x] 接入原生文件保存/选择对话框。
- [x] 配置 electron-builder，产出 `.exe` 安装包和便携 `.zip`。
- [x] 验收：安装后离线可运行，导出文件可保存到本地或网盘同步目录。

### 阶段 E：安卓 APK

- [x] 初始化 Capacitor，指向 `app/` 构建产物。
- [x] 接入 capacitor-sqlite 或 IndexedDB。
- [x] 接入系统文件选择器，支持本地、云盘、网盘目录。
- [x] 设置 `android:allowBackup="false"`，确保卸载即清空。
- [x] 生成调试 APK。
- [x] 编写正式签名说明，用户可自行替换签名密钥。
- [x] 验收：真机安装、导入导出、AI 生成、闯关全流程可用。

### 阶段 F：GitHub 开源与发布

- [x] 初始化 Git 仓库。
- [x] 添加 `.gitignore`，排除 `.env`、密钥、构建产物、数据库文件。
- [x] 添加 MIT LICENSE。
- [x] 编写 README：功能、截图、下载地址、使用说明、构建方法。
- [x] 编写用户使用说明和数据格式说明。
- [x] 添加 `samples/` 示例学习区备份。
- [x] 配置 GitHub Actions：Windows 构建、安卓 APK 构建、自动生成 Release。
- [x] 发布首个 Release，附安装包和 APK。
- [x] 验收：全新用户按 README 即可下载、安装、导入示例、开始使用。

## 8. 验收总清单

- [x] 完全离线可用，不依赖后端、域名、网络（除 AI 请求外）。
- [x] 无登录流程。
- [x] 数据保存在用户设备。
- [x] 删除数据即清空。
- [x] 导出 `.zip` 后可完整恢复。
- [x] 学习区可跨用户导入分享。
- [x] API Key 不会出现在导出文件中。
- [x] Windows 安装包和安卓 APK 均可从 GitHub Releases 下载安装。

## 9. 分工

Codex 可完成：

- 本地数据层、AI 直连、PDF 解析。
- 导入导出、冲突处理、格式版本兼容。
- PWA、Electron、Capacitor 三套构建配置。
- 本地构建与功能验证。
- README、使用文档、数据格式文档、示例数据。
- GitHub Actions 自动构建配置。
- 用户授权后创建仓库、发布 Release。

用户必须手动完成：

- 创建 GitHub 账号和仓库，或授权 Codex 创建。
- 决定开源协议（建议 MIT）。
- 保管安卓正式签名密钥；前期可用调试包测试。
- 在真实 Windows 电脑和安卓手机上做安装测试。
- 提供自己的 AI API Key 验证生成功能。

## 10. 风险与预案

- 用户没有 AI Key 时无法生成新卡片：提供示例学习区，并考虑后续加“手动建卡”。
- Windows 未签名安装包有 SmartScreen 提示：文档中说明，也可选买代码签名证书。
- 安卓 APK 有“未知来源”提示：文档中说明安装方法。
- 浏览器可能清除 PWA 数据：应用内强化“定期导出备份”提醒。
- PDF 扫描件无法提取文字：保持现有提示，文档中说明支持范围。

## 11. 开工顺序

1. 先完成阶段 A，做出可离线的单页应用，不急着打包。
2. 阶段 A 验证通过后做阶段 B 导入导出。
3. 阶段 C 先出 PWA，让用户在浏览器里即可体验。
4. 阶段 D 和阶段 E 分别打包 Windows 与安卓。
5. 最后做阶段 F 开源发布。

任何阶段遇到阻塞时，不要绕过本方案擅自改变产品决策；先记录问题并回到本文档确认。
